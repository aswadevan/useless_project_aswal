# Lightweight Python dependencies for ARIYATHE THALAYATTIYATH ML Pipeline
# Optimised for: 12th-gen Intel CPU, 8 GB RAM, no GPU, Windows 11
#
# Install with:
#   pip install -r requirements.txt
#
# All packages are CPU-only, commonly available, and train in seconds on small tabular data.

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3
